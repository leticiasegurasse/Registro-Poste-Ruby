<%= form_with(model: bairro) do |form| %>
  <% if bairro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bairro.errors.count, "error") %> prohibited this bairro from being saved:</h2>

      <ul>
        <% bairro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :cidade_id, style: "display: block" %>
    <%= form.text_field :cidade_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
